'use strict';

const numbers = require('./numbers');
const products = require('./products');

var primeList = numbers.primeNumbers();   // n sqrt n
// var productValues = products.primeProducts(primeList); // n*n
// var result = products.printPrimeNumbers(productValues); 

process.stdout.write(result);